# logcut v0.1.8

- 独自FFI宣言を `libc` の型と関数へ置き換えました。
- シグナル処理を `sigaction()` ベースに変更し、終了時に以前のハンドラを復元するようにしました。
- PHPCBFの終了判定を専用モジュールへ整理しました。
- `LOGCUT_LOG_MAX_AGE_DAYS` と実際の保持日数のずれを修正しました。
- テスト用一時ディレクトリを自動削除する共通ヘルパーを追加しました。

CLI出力、終了コード、シグナル転送、ログ保存の既存仕様は維持しています。
